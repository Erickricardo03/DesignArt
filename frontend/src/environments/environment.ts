// Ambiente de DESENVOLVIMENTO (usado por `ng serve` e por builds sem a
// configuração "production"). O `angular.json` troca este arquivo pelo
// environment.prod.ts automaticamente ao rodar `ng build --configuration production`.
export const environment = {
  production: false,
};
